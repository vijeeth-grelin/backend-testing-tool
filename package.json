{
  "name": "api-testing-tool",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3",
    "zod": "^4.4.3"
  }
}
